
/**
 * Module dependencies.
 */

var connect = require('../');

// $ curl -i http://graphgist-cms-api.herokuapp.com/favicon.ico
// true defaults to 1000ms

connect.createServer(
    connect.logger({ buffer: 5000 })
  , connect.favicon()
).listen(3000);